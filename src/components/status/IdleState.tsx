export function IdleState() {
    return (
      <div className="idle-state">
        <span className="idle-state__icon">🍔</span>
        <p className="idle-state__text">Enter a postcode to find restaurants near you</p>
      </div>
    );
  }